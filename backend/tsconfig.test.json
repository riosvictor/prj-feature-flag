{
	"compilerOptions": {
		"baseUrl": "src",
		"paths": {
			".": ["."]
		}
	},
	"exclude": ["coverage", "dist", "node_modules"],
	"extends": "./tsconfig.json",
	"include": [".", "src/**/*.spec.ts"]
}
